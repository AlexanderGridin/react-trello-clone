import styled from "styled-components";
import { ErrorButton } from "shared/components/Buttons/ErrorButton";

export const RemoveCardButton = styled(ErrorButton)`
  margin-left: auto;
  margin-right: 0;
  padding: 6px;
`;
